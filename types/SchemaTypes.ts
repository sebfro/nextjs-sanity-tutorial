export type Pet = {
    _createdAt: string,
    _id: string,
    _rev: string,
    _type: string,
    _updatedAt: string,
    name: string
}

export type Workout = {
    _createdAt: string,
    _id: string,
    name: string,
    reps: string[]
}

export type WorkoutWeek = {
    _createdAt: string,
    _id: string,
    name: string
}